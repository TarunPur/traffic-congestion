export default function Home() {
  return (
    <main style={{ padding: "20px" }}>
      <p style={{ fontFamily: "system-ui, sans-serif", color: "var(--ink)" }}>
        Clearline — scaffold running.
      </p>
    </main>
  );
}
